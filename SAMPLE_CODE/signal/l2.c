
#include <stdio.h>
#include <signal.h>

/* When a stop signal arrives, set the action back to the default
   and then resend the signal after doing cleanup actions. */

void sighand(int sig){
	system	
sigkill();
}

int
main (void)
{
	for(long int i=0; i<=123213; i++){}
	signal(SIGINT,sighand);

}


