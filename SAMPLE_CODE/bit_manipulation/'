#include <stdio.h> 

void show_mem_rep(char *start, int n) 
{ 
	int i; 
	for (i = 0; i < n; i++) 
		printf(" %.2x", start[i]); 
	printf("\n"); 
} 

int main() 
{ 
	int i = 0x01234567; 
	void * a = "asf";
//	printf("%s\n",a);
	//show_mem_rep((void *)&i, sizeof(i)); 
	getchar(); 
	return 0; 
} 

